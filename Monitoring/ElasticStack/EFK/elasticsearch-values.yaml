replicas: 1
minimumMasterNodes: 1
# Resource limits for demo (adjust for production)
resources:
  requests:
    cpu: "500m"
    memory: "2Gi"
  limits:
    cpu: "1000m"
    memory: "2Gi"

# Persistent storage
volumeClaimTemplate:
  accessModes: [ "ReadWriteOnce" ]
  resources:
    requests:
      storage: 10Gi

# Security
secret:
  password: "changeme"  # Change this!

# Disable TLS for demo
protocol: http